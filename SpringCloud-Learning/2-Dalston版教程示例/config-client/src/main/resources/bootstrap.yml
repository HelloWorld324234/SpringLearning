spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:1201/
      profile: default
      label: dev

server:
  port: 7002
eureka:
  client:
    serviceUrl:
      defauleZone: http://localhost:1111/eureka/


